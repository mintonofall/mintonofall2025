// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_PRISMA_URL")
  directUrl = env("POSTGRES_URL_NON_POOLING")
}

model User {
  id      Int      @id @default(autoincrement())
  userName String   @unique
  password String
  point Int @default(10000)
  clubs Club[] @relation("UserClubs")


}

model Club {
  id Int @id @default(autoincrement())
  clubName String @unique
  clubLogo String?
  clubDescription String?
  clubLocation String?
  users User[] @relation("UserClubs")
  howManyCourts Int @default(3)
  players Player[]

  

}

model Player {
  id Int @id @default(autoincrement())
  name String 
  grade String
  gender String @default("man")
  age Int
  avater String?
  clubid Int
  club Club @relation(fields: [clubid], references: [id])
  suttlePoint Int @default(10000)
  waitPlayerLists WaitPlayerList[]
  waitGames WaitGame[]
  win Int @default(0)
  lose Int @default(0)
  games Int @default(0)
  mmr Int @default(1000)
  gameDatas DateTime[]
  winDatas String[]
  loseDatas String[]
  enterDatas DateTime[]
  createat DateTime @default(now())


}

model WaitPlayerList {
  id Int @id @default(autoincrement())
  clubid Int
  Playerid Int
  player Player @relation(fields: [Playerid], references: [id])
  enterDate DateTime
  exitDate DateTime?
}

model WaitGame {
  id Int @id @default(autoincrement())
  clubid Int
  playerid Int
  player Player @relation(fields: [playerid], references: [id])
  point Int
  updateTime DateTime? @updatedAt
}

model Match {
  id Int @id @default(autoincrement())
  gameid String 
  player1id Int
  player2id Int
  player3id Int
  player4id Int
  clubid Int
  winner1id Int?
  winner2id Int?
  startTime DateTime @default(now())
  endTime DateTime?
  duration Int?
  score1 Int?
  score2 Int?
}

model GameBoard {
  id Int @id @default(autoincrement())
  gameid String 
  clubid Int
  CourtNumber Int
  updateTime DateTime? @updatedAt
  player1id Int
  player2id Int
  player3id Int
  player4id Int
}